stages:
  - lint
  - build
  - test
  - release


lint:
  stage: lint
  image: golangci/golangci-lint:latest
  script:
    - golangci-lint run


build:
  stage: build
  image: golang:latest
  script:
    - go mod download
    - go build ./cmd/banforge/main.go -o banforge


test:
  stage: test
  image: golang:latest
  script:
    - go test
 
